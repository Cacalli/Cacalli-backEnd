const User = require("../../models/user").model;
const User = require("../../models/pickup").model;

const getAllPayments = (data) => {

};

const getLastPayment = (data) => {

};

module.exports = {
    getAllPayments,
    getLastPayment,
}